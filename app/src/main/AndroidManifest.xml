<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.online.kios.dkiosonline">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_dkios"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_dkios"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewListBarangActivity"></activity>
        <activity android:name=".ViewDetailBarangActivity"></activity>
        <activity android:name=".ListTroliActivity"></activity>
    </application>

</manifest>